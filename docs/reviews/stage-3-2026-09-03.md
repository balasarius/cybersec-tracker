# Stage 3 review — 2026-09-03

- Approval state: In progress; first slice pending human review
- Reviewer: Unassigned

## Scope and decisions

The first Stage 3 slice establishes provider accounts/import runs, immutable versioned raw records, canonical assets and source aliases, explicit ambiguity cases, normalized provider findings, identifiers, and deliberately separate manual issues. IP addresses remain weak identities and always require analyst review. Cross-source canonical observations and remediation grouping remain Stage 6 concerns.

## Evidence

- `apps/integrations/models.py` and `services.py`
- `apps/assets/models.py` and `services.py`
- `apps/findings/models.py` and `services.py`
- initial reversible migrations for all three modules
- ingestion, identity-resolution, provenance, permission, manual-issue, and database-constraint tests

## Test results

Pending PostgreSQL CI execution for this slice.

## Risks and deferred work

- Asset merge/split operations and complete alias-history tooling remain within Stage 3.
- Ownership rules, OWASP assessment, versioned policy clocks, tasks, evidence, and Security oversight are subsequent Stage 3 slices.
- Raw-record retention deletion will require a privileged, previewed retention operation; records are immutable through ordinary model updates.
- Provider severity remains explicitly separate from OWASP task risk.
- No automatic cross-source merge exists in this slice.

## Gate assessment

Stage 3 remains in progress. Do not approve the stage based on this slice alone.
