# Stage 2 review — 2026-09-03

- Approval state: Pending implementation evidence and human review
- Reviewer: Unassigned

## Scope and decisions

This stage introduces stable UUID users and external identities; organisations, business units, teams, roles and grants; sensitive-unit access rules; Argon2 password hashing, reset flows, login throttling and TOTP support; request correlation; and per-organisation append-only hash-chained audit events.

## Evidence

- `apps/accounts/`, `apps/tenancy/`, and `apps/audit/`
- migrations for the custom user, tenancy model, audit model, and database immutability trigger
- ADR 0006 scoped authorization and ADR 0007 hash-chained audit
- authentication, permission, isolation, correlation, redaction, and integrity tests

## Test results

GitHub Actions run `33694539035` passed on 2026-09-03:

- Ruff formatting and lint passed.
- strict mypy passed for 46 source files.
- migration consistency reported no changes.
- 28 PostgreSQL-backed tests passed in 3.08 seconds.
- statement/branch coverage reported 95%, above the configured threshold.
- `pip-audit` found no known vulnerabilities and the repository secret scan passed.
- the production container build passed.

## Risks and deferred work

- TOTP enrollment and recovery-code user interfaces remain to be delivered before this stage can exit.
- Privileged-read/export audit calls must be connected to each endpoint as those endpoints are added.
- Team/task scope will be enforced when task records are introduced; the membership boundary is established here.
- Nested business-unit grant inheritance is intentionally absent.
- Session invalidation after role/grant change and administrator recovery procedures require tests before stage exit.

## Gate assessment

Pending. This change is not production-ready and must not be treated as approval of later domain workflows.
